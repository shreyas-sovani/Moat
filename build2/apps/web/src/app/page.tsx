export default function Page() {
	return (
		<main>
			<p className="kicker">Base Sepolia · testnet</p>
			<h1>Moat</h1>
			<p className="lede">Liquidation protection that plans with AI and executes without it.</p>
			<p>
				The agent plans, you approve, the chain executes. KeeperHub is the execution layer — nothing
				is inferred at run time.
			</p>
			<p>
				<a href="/dashboard">Enter App</a>
			</p>
		</main>
	);
}
